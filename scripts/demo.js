// demo.js — Hivecode live coordination test
// Created by Claude (AI agent) via MCP, claimed before editing.

function greet(name) {
  return `Hello, ${name}! Welcome to the hive.`;
}

module.exports = { greet };
